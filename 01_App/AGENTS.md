# 应用层规则

- 应用层只负责启动流程、任务编排和业务状态，不直接调用 HAL、BSP 或 `05_Impl`。
- 通过 `02_Service` 的公开接口访问业务能力；不要把硬件句柄、GPIO 端口或 DMA 缓冲区暴露到应用层。
- 任务创建、优先级、栈大小和周期变更属于实时性变更，必须说明原因并验证。
- 允许使用 FreeRTOS/CMSIS-OS2 任务控制接口与通用日志中间件；不得使用 `plat_*`、CubeMX 或 HAL 接口。
