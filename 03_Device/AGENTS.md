# 设备层规则

- 设备层提供 `device_*` 语义 API，负责参数校验和设备状态；可依赖 `04_Platform` 的公开接口，不得依赖 `05_Impl` 或 HAL。
- 新设备的头文件放在 `Inc/`，实现放在 `Src/`，并同步更新本目录 `CMakeLists.txt`。
- 将板级 ID、具体引脚和协议细节留在 Platform 层；设备层不得向上暴露这些实现细节。
